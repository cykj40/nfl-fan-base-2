const User = require('./user');
const Comment = require('./comment');
const Post = require('./post'); // Import the Post model
const Reaction = require('./reaction'); // Import the Reaction model

module.exports = { User, Comment, Post, Reaction }; 